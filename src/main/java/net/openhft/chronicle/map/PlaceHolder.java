package net.openhft.chronicle.map;


public class PlaceHolder {

    // just created as a place holder as the javadoc is failing with public classes
    // can be removed once we have created public classes

    public PlaceHolder() {
    }
}
